{
  "name": "sally-ai",
  "version": "1.0.0",
  "description": "Sally.ai - 린 고객개발 인터뷰 질문 생성 AI",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && node src/app.js",
    "dev:frontend": "cd frontend && npx expo start --web --clear"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
